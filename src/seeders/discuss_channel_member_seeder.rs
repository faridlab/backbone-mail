//! DiscussChannelMember seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial DiscussChannelMember data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// DISCUSSCHANNELMEMBER SEEDER
// ============================================================================

/// Seeder for discuss_channel_member
pub struct SeedDiscussChannelMemberSeeder;

impl SeedDiscussChannelMemberSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedDiscussChannelMemberSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedDiscussChannelMemberSeeder {
    fn name(&self) -> &'static str {
        "SeedDiscussChannelMemberSeeder"
    }

    fn order(&self) -> i32 {
        19
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if messaging.discuss_channel_members table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM messaging.discuss_channel_members")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/discuss_channel_member_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM messaging.discuss_channel_members")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
