//! MailGatewayAllowed seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial MailGatewayAllowed data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// MAILGATEWAYALLOWED SEEDER
// ============================================================================

/// Seeder for mail_gateway_allowed
pub struct SeedMailGatewayAllowedSeeder;

impl SeedMailGatewayAllowedSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedMailGatewayAllowedSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedMailGatewayAllowedSeeder {
    fn name(&self) -> &'static str {
        "SeedMailGatewayAllowedSeeder"
    }

    fn order(&self) -> i32 {
        23
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if messaging.mail_gateway_allowed table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM messaging.mail_gateway_allowed")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/mail_gateway_allowed_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM messaging.mail_gateway_allowed")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

