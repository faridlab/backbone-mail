//! MailMessageSchedule seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial MailMessageSchedule data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// MAILMESSAGESCHEDULE SEEDER
// ============================================================================

/// Seeder for mail_message_schedule
pub struct SeedMailMessageScheduleSeeder;

impl SeedMailMessageScheduleSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedMailMessageScheduleSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedMailMessageScheduleSeeder {
    fn name(&self) -> &'static str {
        "SeedMailMessageScheduleSeeder"
    }

    fn order(&self) -> i32 {
        21
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if messaging.mail_message_schedules table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM messaging.mail_message_schedules")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/mail_message_schedule_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM messaging.mail_message_schedules")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

