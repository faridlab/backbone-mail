//! MailAliasDomain seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial MailAliasDomain data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// MAILALIASDOMAIN SEEDER
// ============================================================================

/// Seeder for mail_alias_domain
pub struct SeedMailAliasDomainSeeder;

impl SeedMailAliasDomainSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedMailAliasDomainSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedMailAliasDomainSeeder {
    fn name(&self) -> &'static str {
        "SeedMailAliasDomainSeeder"
    }

    fn order(&self) -> i32 {
        5
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if messaging.mail_alias_domains table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM messaging.mail_alias_domains")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/mail_alias_domain_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM messaging.mail_alias_domains")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

