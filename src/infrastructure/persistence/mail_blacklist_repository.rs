//! Repository for MailBlacklist entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<MailBlacklist, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::MailBlacklist;

/// Table name for MailBlacklist entities
pub const TABLE_NAME: &str = "messaging.mail_blacklists";

/// Repository for MailBlacklist entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct MailBlacklistRepository(
    backbone_orm::GenericCrudRepository<MailBlacklist, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for MailBlacklistRepository {
    type Target = backbone_orm::GenericCrudRepository<MailBlacklist, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl MailBlacklistRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl MailBlacklistRepository {
    /// Find mail_blacklist by email (excludes soft-deleted)
    pub async fn find_by_email(&self, email: &str) -> Result<Option<MailBlacklist>> {
        self.find_by_text_field("email", email).await
    }

    /// Check if mail_blacklist exists by email (excludes soft-deleted)
    pub async fn exists_by_email(&self, email: &str) -> Result<bool> {
        self.exists_by_text_field("email", email).await
    }

}

backbone_core::impl_crud_repository!(MailBlacklistRepository, MailBlacklist, soft_delete);
