//! Repository for Sms entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Sms, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Sms;

/// Table name for Sms entities
pub const TABLE_NAME: &str = "messaging.sms";

/// Repository for Sms entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct SmsRepository(
    backbone_orm::GenericCrudRepository<Sms, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for SmsRepository {
    type Target = backbone_orm::GenericCrudRepository<Sms, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl SmsRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl SmsRepository {
    /// Find sms by uuid (excludes soft-deleted)
    pub async fn find_by_uuid(&self, uuid: &str) -> Result<Option<Sms>> {
        self.find_by_text_field("uuid", uuid).await
    }

    /// Check if sms exists by uuid (excludes soft-deleted)
    pub async fn exists_by_uuid(&self, uuid: &str) -> Result<bool> {
        self.exists_by_text_field("uuid", uuid).await
    }

}

backbone_core::impl_crud_repository!(SmsRepository, Sms, soft_delete);
