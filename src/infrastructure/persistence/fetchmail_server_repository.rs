//! Repository for FetchmailServer entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<FetchmailServer, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::FetchmailServer;

/// Table name for FetchmailServer entities
pub const TABLE_NAME: &str = "messaging.fetchmail_servers";

/// Repository for FetchmailServer entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct FetchmailServerRepository(
    backbone_orm::GenericCrudRepository<FetchmailServer, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for FetchmailServerRepository {
    type Target = backbone_orm::GenericCrudRepository<FetchmailServer, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl FetchmailServerRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(FetchmailServerRepository, FetchmailServer, soft_delete);
