//! Repository for PhoneBlacklist entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<PhoneBlacklist, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::PhoneBlacklist;

/// Table name for PhoneBlacklist entities
pub const TABLE_NAME: &str = "messaging.phone_blacklists";

/// Repository for PhoneBlacklist entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct PhoneBlacklistRepository(
    backbone_orm::GenericCrudRepository<PhoneBlacklist, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for PhoneBlacklistRepository {
    type Target = backbone_orm::GenericCrudRepository<PhoneBlacklist, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl PhoneBlacklistRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl PhoneBlacklistRepository {
    /// Find phone_blacklist by number (excludes soft-deleted)
    pub async fn find_by_number(&self, number: &str) -> Result<Option<PhoneBlacklist>> {
        self.find_by_text_field("number", number).await
    }

    /// Check if phone_blacklist exists by number (excludes soft-deleted)
    pub async fn exists_by_number(&self, number: &str) -> Result<bool> {
        self.exists_by_text_field("number", number).await
    }

}

backbone_core::impl_crud_repository!(PhoneBlacklistRepository, PhoneBlacklist, soft_delete);
