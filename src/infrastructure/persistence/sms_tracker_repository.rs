//! Repository for SmsTracker entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<SmsTracker, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::SmsTracker;

/// Table name for SmsTracker entities
pub const TABLE_NAME: &str = "messaging.sms_trackers";

/// Repository for SmsTracker entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct SmsTrackerRepository(
    backbone_orm::GenericCrudRepository<SmsTracker, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for SmsTrackerRepository {
    type Target = backbone_orm::GenericCrudRepository<SmsTracker, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl SmsTrackerRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl SmsTrackerRepository {
    /// Find sms_tracker by sms_uuid (excludes soft-deleted)
    pub async fn find_by_sms_uuid(&self, sms_uuid: &str) -> Result<Option<SmsTracker>> {
        self.find_by_text_field("sms_uuid", sms_uuid).await
    }

    /// Check if sms_tracker exists by sms_uuid (excludes soft-deleted)
    pub async fn exists_by_sms_uuid(&self, sms_uuid: &str) -> Result<bool> {
        self.exists_by_text_field("sms_uuid", sms_uuid).await
    }

}

backbone_core::impl_crud_repository!(SmsTrackerRepository, SmsTracker, soft_delete);
