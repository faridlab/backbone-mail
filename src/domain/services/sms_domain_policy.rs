//! Domain policy for Sms aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::Sms;

/// Domain policy for Sms — enforces business invariants.
pub struct SmsDomainPolicy;

#[async_trait]
impl DomainPolicy<Sms> for SmsDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &Sms) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked sms cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}
