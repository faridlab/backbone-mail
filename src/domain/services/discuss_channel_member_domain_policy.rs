//! Domain policy for DiscussChannelMember aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::DiscussChannelMember;

/// Domain policy for DiscussChannelMember — enforces business invariants.
pub struct DiscussChannelMemberDomainPolicy;

#[async_trait]
impl DomainPolicy<DiscussChannelMember> for DiscussChannelMemberDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &DiscussChannelMember) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked discuss_channel_member cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}
