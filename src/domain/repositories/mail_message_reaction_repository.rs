//! Repository trait for MailMessageReaction aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailMessageReaction aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailMessageReaction;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailMessageReactionPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailMessageReactionPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailMessageReactionPaginatedResult {
    pub data: Vec<MailMessageReaction>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailMessageReactionFilter {
    pub message_id: Option<Uuid>,
    pub content: Option<String>,
    pub partner_id: Option<Uuid>,
    pub guest_id: Option<Uuid>,
}

impl MailMessageReactionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.message_id.is_some() || self.content.is_some() || self.partner_id.is_some() || self.guest_id.is_some()
    }
}

/// Repository trait for MailMessageReaction aggregate
///
/// This trait defines the persistence contract for MailMessageReaction entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailMessageReactionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_message_reaction entity
    async fn save(&self, entity: &MailMessageReaction) -> Result<MailMessageReaction>;

    /// Find mail_message_reaction by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailMessageReaction>>;

    /// Find all mail_message_reaction entities
    async fn find_all(&self) -> Result<Vec<MailMessageReaction>>;

    /// Update mail_message_reaction by ID
    async fn update(&self, id: &str, entity: &MailMessageReaction) -> Result<Option<MailMessageReaction>>;

    /// Delete mail_message_reaction by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_message_reaction with pagination
    async fn list(&self, params: MailMessageReactionPaginationParams) -> Result<MailMessageReactionPaginatedResult>;

    /// List mail_message_reaction with pagination and filters
    async fn list_with_filters(&self, params: MailMessageReactionPaginationParams, filters: MailMessageReactionFilter) -> Result<MailMessageReactionPaginatedResult>;

    /// Count all mail_message_reaction entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_message_reaction entities matching filters
    async fn count_with_filters(&self, filters: MailMessageReactionFilter) -> Result<u64>;

    /// Check if mail_message_reaction exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_message_reaction by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_message_reaction by ID
    async fn restore(&self, id: &str) -> Result<Option<MailMessageReaction>>;

    /// List soft-deleted mail_message_reaction entities
    async fn list_deleted(&self, params: MailMessageReactionPaginationParams) -> Result<MailMessageReactionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_message_reaction entities
    async fn bulk_save(&self, entities: &[MailMessageReaction]) -> Result<Vec<MailMessageReaction>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
