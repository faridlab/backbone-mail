//! Repository trait for MailServer aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailServer aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{MailServer, SmtpAuthentication, SmtpEncryption};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailServerPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailServerPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailServerPaginatedResult {
    pub data: Vec<MailServer>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailServerFilter {
    pub name: Option<String>,
    pub from_filter: Option<String>,
    pub smtp_host: Option<String>,
    pub smtp_authentication: Option<SmtpAuthentication>,
    pub smtp_user: Option<String>,
    pub smtp_pass_ref: Option<String>,
    pub smtp_encryption: Option<SmtpEncryption>,
    pub smtp_debug: Option<bool>,
    pub active: Option<bool>,
}

impl MailServerFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.from_filter.is_some() || self.smtp_host.is_some() || self.smtp_authentication.is_some() || self.smtp_user.is_some() || self.smtp_pass_ref.is_some() || self.smtp_encryption.is_some() || self.smtp_debug.is_some() || self.active.is_some()
    }
}

/// Repository trait for MailServer aggregate
///
/// This trait defines the persistence contract for MailServer entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailServerRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_server entity
    async fn save(&self, entity: &MailServer) -> Result<MailServer>;

    /// Find mail_server by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailServer>>;

    /// Find all mail_server entities
    async fn find_all(&self) -> Result<Vec<MailServer>>;

    /// Update mail_server by ID
    async fn update(&self, id: &str, entity: &MailServer) -> Result<Option<MailServer>>;

    /// Delete mail_server by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_server with pagination
    async fn list(&self, params: MailServerPaginationParams) -> Result<MailServerPaginatedResult>;

    /// List mail_server with pagination and filters
    async fn list_with_filters(&self, params: MailServerPaginationParams, filters: MailServerFilter) -> Result<MailServerPaginatedResult>;

    /// Count all mail_server entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_server entities matching filters
    async fn count_with_filters(&self, filters: MailServerFilter) -> Result<u64>;

    /// Check if mail_server exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_server by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_server by ID
    async fn restore(&self, id: &str) -> Result<Option<MailServer>>;

    /// List soft-deleted mail_server entities
    async fn list_deleted(&self, params: MailServerPaginationParams) -> Result<MailServerPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_server entities
    async fn bulk_save(&self, entities: &[MailServer]) -> Result<Vec<MailServer>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
