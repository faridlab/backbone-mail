//! Repository trait for MailAttachment aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailAttachment aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailAttachment;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailAttachmentPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailAttachmentPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailAttachmentPaginatedResult {
    pub data: Vec<MailAttachment>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailAttachmentFilter {
    pub name: Option<String>,
    pub mimetype: Option<String>,
    pub datas: Option<String>,
    pub checksum: Option<String>,
    pub access_token: Option<Uuid>,
    pub owner_party_id: Option<Uuid>,
    pub owner_guest_id: Option<Uuid>,
}

impl MailAttachmentFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.mimetype.is_some() || self.datas.is_some() || self.checksum.is_some() || self.access_token.is_some() || self.owner_party_id.is_some() || self.owner_guest_id.is_some()
    }
}

/// Repository trait for MailAttachment aggregate
///
/// This trait defines the persistence contract for MailAttachment entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailAttachmentRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_attachment entity
    async fn save(&self, entity: &MailAttachment) -> Result<MailAttachment>;

    /// Find mail_attachment by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailAttachment>>;

    /// Find all mail_attachment entities
    async fn find_all(&self) -> Result<Vec<MailAttachment>>;

    /// Update mail_attachment by ID
    async fn update(&self, id: &str, entity: &MailAttachment) -> Result<Option<MailAttachment>>;

    /// Delete mail_attachment by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_attachment with pagination
    async fn list(&self, params: MailAttachmentPaginationParams) -> Result<MailAttachmentPaginatedResult>;

    /// List mail_attachment with pagination and filters
    async fn list_with_filters(&self, params: MailAttachmentPaginationParams, filters: MailAttachmentFilter) -> Result<MailAttachmentPaginatedResult>;

    /// Count all mail_attachment entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_attachment entities matching filters
    async fn count_with_filters(&self, filters: MailAttachmentFilter) -> Result<u64>;

    /// Check if mail_attachment exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_attachment by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_attachment by ID
    async fn restore(&self, id: &str) -> Result<Option<MailAttachment>>;

    /// List soft-deleted mail_attachment entities
    async fn list_deleted(&self, params: MailAttachmentPaginationParams) -> Result<MailAttachmentPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find mail_attachment by access_token
    async fn find_by_access_token(&self, access_token: Option<Uuid>) -> Result<Option<MailAttachment>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if mail_attachment exists by access_token
    async fn exists_by_access_token(&self, access_token: Option<Uuid>) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_attachment entities
    async fn bulk_save(&self, entities: &[MailAttachment]) -> Result<Vec<MailAttachment>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
