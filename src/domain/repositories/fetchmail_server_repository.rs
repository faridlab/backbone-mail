//! Repository trait for FetchmailServer aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the FetchmailServer aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{FetchmailServer, FetchmailState};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct FetchmailServerPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl FetchmailServerPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct FetchmailServerPaginatedResult {
    pub data: Vec<FetchmailServer>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct FetchmailServerFilter {
    pub name: Option<String>,
    pub active: Option<bool>,
    pub state: Option<FetchmailState>,
    pub default_thread_model: Option<String>,
    pub token_hash: Option<String>,
    pub error_message: Option<String>,
}

impl FetchmailServerFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.active.is_some() || self.state.is_some() || self.default_thread_model.is_some() || self.token_hash.is_some() || self.error_message.is_some()
    }
}

/// Repository trait for FetchmailServer aggregate
///
/// This trait defines the persistence contract for FetchmailServer entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait FetchmailServerRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new fetchmail_server entity
    async fn save(&self, entity: &FetchmailServer) -> Result<FetchmailServer>;

    /// Find fetchmail_server by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<FetchmailServer>>;

    /// Find all fetchmail_server entities
    async fn find_all(&self) -> Result<Vec<FetchmailServer>>;

    /// Update fetchmail_server by ID
    async fn update(&self, id: &str, entity: &FetchmailServer) -> Result<Option<FetchmailServer>>;

    /// Delete fetchmail_server by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List fetchmail_server with pagination
    async fn list(&self, params: FetchmailServerPaginationParams) -> Result<FetchmailServerPaginatedResult>;

    /// List fetchmail_server with pagination and filters
    async fn list_with_filters(&self, params: FetchmailServerPaginationParams, filters: FetchmailServerFilter) -> Result<FetchmailServerPaginatedResult>;

    /// Count all fetchmail_server entities
    async fn count(&self) -> Result<u64>;

    /// Count fetchmail_server entities matching filters
    async fn count_with_filters(&self, filters: FetchmailServerFilter) -> Result<u64>;

    /// Check if fetchmail_server exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete fetchmail_server by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted fetchmail_server by ID
    async fn restore(&self, id: &str) -> Result<Option<FetchmailServer>>;

    /// List soft-deleted fetchmail_server entities
    async fn list_deleted(&self, params: FetchmailServerPaginationParams) -> Result<FetchmailServerPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save fetchmail_server entities
    async fn bulk_save(&self, entities: &[FetchmailServer]) -> Result<Vec<FetchmailServer>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
