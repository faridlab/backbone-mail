//! Repository trait for MailMessageStar aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailMessageStar aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailMessageStar;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailMessageStarPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailMessageStarPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailMessageStarPaginatedResult {
    pub data: Vec<MailMessageStar>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailMessageStarFilter {
    pub partner_id: Option<Uuid>,
    pub message_id: Option<Uuid>,
}

impl MailMessageStarFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.partner_id.is_some() || self.message_id.is_some()
    }
}

/// Repository trait for MailMessageStar aggregate
///
/// This trait defines the persistence contract for MailMessageStar entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailMessageStarRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_message_star entity
    async fn save(&self, entity: &MailMessageStar) -> Result<MailMessageStar>;

    /// Find mail_message_star by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailMessageStar>>;

    /// Find all mail_message_star entities
    async fn find_all(&self) -> Result<Vec<MailMessageStar>>;

    /// Update mail_message_star by ID
    async fn update(&self, id: &str, entity: &MailMessageStar) -> Result<Option<MailMessageStar>>;

    /// Delete mail_message_star by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_message_star with pagination
    async fn list(&self, params: MailMessageStarPaginationParams) -> Result<MailMessageStarPaginatedResult>;

    /// List mail_message_star with pagination and filters
    async fn list_with_filters(&self, params: MailMessageStarPaginationParams, filters: MailMessageStarFilter) -> Result<MailMessageStarPaginatedResult>;

    /// Count all mail_message_star entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_message_star entities matching filters
    async fn count_with_filters(&self, filters: MailMessageStarFilter) -> Result<u64>;

    /// Check if mail_message_star exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_message_star by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_message_star by ID
    async fn restore(&self, id: &str) -> Result<Option<MailMessageStar>>;

    /// List soft-deleted mail_message_star entities
    async fn list_deleted(&self, params: MailMessageStarPaginationParams) -> Result<MailMessageStarPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_message_star entities
    async fn bulk_save(&self, entities: &[MailMessageStar]) -> Result<Vec<MailMessageStar>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
