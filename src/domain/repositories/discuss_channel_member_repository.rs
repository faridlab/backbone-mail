//! Repository trait for DiscussChannelMember aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the DiscussChannelMember aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{DiscussChannelMember, MemberCustomNotifications, SidebarFoldState};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DiscussChannelMemberPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl DiscussChannelMemberPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct DiscussChannelMemberPaginatedResult {
    pub data: Vec<DiscussChannelMember>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DiscussChannelMemberFilter {
    pub channel_id: Option<Uuid>,
    pub partner_id: Option<Uuid>,
    pub guest_id: Option<Uuid>,
    pub fold_state: Option<SidebarFoldState>,
    pub fetched_message_id: Option<Uuid>,
    pub new_message_separator: Option<Uuid>,
    pub custom_notifications: Option<MemberCustomNotifications>,
    pub seen_message_id: Option<Uuid>,
    pub is_pinned: Option<bool>,
}

impl DiscussChannelMemberFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.channel_id.is_some() || self.partner_id.is_some() || self.guest_id.is_some() || self.fold_state.is_some() || self.fetched_message_id.is_some() || self.new_message_separator.is_some() || self.custom_notifications.is_some() || self.seen_message_id.is_some() || self.is_pinned.is_some()
    }
}

/// Repository trait for DiscussChannelMember aggregate
///
/// This trait defines the persistence contract for DiscussChannelMember entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait DiscussChannelMemberRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new discuss_channel_member entity
    async fn save(&self, entity: &DiscussChannelMember) -> Result<DiscussChannelMember>;

    /// Find discuss_channel_member by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<DiscussChannelMember>>;

    /// Find all discuss_channel_member entities
    async fn find_all(&self) -> Result<Vec<DiscussChannelMember>>;

    /// Update discuss_channel_member by ID
    async fn update(&self, id: &str, entity: &DiscussChannelMember) -> Result<Option<DiscussChannelMember>>;

    /// Delete discuss_channel_member by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List discuss_channel_member with pagination
    async fn list(&self, params: DiscussChannelMemberPaginationParams) -> Result<DiscussChannelMemberPaginatedResult>;

    /// List discuss_channel_member with pagination and filters
    async fn list_with_filters(&self, params: DiscussChannelMemberPaginationParams, filters: DiscussChannelMemberFilter) -> Result<DiscussChannelMemberPaginatedResult>;

    /// Count all discuss_channel_member entities
    async fn count(&self) -> Result<u64>;

    /// Count discuss_channel_member entities matching filters
    async fn count_with_filters(&self, filters: DiscussChannelMemberFilter) -> Result<u64>;

    /// Check if discuss_channel_member exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete discuss_channel_member by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted discuss_channel_member by ID
    async fn restore(&self, id: &str) -> Result<Option<DiscussChannelMember>>;

    /// List soft-deleted discuss_channel_member entities
    async fn list_deleted(&self, params: DiscussChannelMemberPaginationParams) -> Result<DiscussChannelMemberPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save discuss_channel_member entities
    async fn bulk_save(&self, entities: &[DiscussChannelMember]) -> Result<Vec<DiscussChannelMember>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
