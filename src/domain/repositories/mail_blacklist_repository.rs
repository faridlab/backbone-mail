//! Repository trait for MailBlacklist aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailBlacklist aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::MailBlacklist;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailBlacklistPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailBlacklistPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailBlacklistPaginatedResult {
    pub data: Vec<MailBlacklist>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailBlacklistFilter {
    pub email: Option<String>,
    pub active: Option<bool>,
}

impl MailBlacklistFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.email.is_some() || self.active.is_some()
    }
}

/// Repository trait for MailBlacklist aggregate
///
/// This trait defines the persistence contract for MailBlacklist entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailBlacklistRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_blacklist entity
    async fn save(&self, entity: &MailBlacklist) -> Result<MailBlacklist>;

    /// Find mail_blacklist by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailBlacklist>>;

    /// Find all mail_blacklist entities
    async fn find_all(&self) -> Result<Vec<MailBlacklist>>;

    /// Update mail_blacklist by ID
    async fn update(&self, id: &str, entity: &MailBlacklist) -> Result<Option<MailBlacklist>>;

    /// Delete mail_blacklist by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_blacklist with pagination
    async fn list(&self, params: MailBlacklistPaginationParams) -> Result<MailBlacklistPaginatedResult>;

    /// List mail_blacklist with pagination and filters
    async fn list_with_filters(&self, params: MailBlacklistPaginationParams, filters: MailBlacklistFilter) -> Result<MailBlacklistPaginatedResult>;

    /// Count all mail_blacklist entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_blacklist entities matching filters
    async fn count_with_filters(&self, filters: MailBlacklistFilter) -> Result<u64>;

    /// Check if mail_blacklist exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_blacklist by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_blacklist by ID
    async fn restore(&self, id: &str) -> Result<Option<MailBlacklist>>;

    /// List soft-deleted mail_blacklist entities
    async fn list_deleted(&self, params: MailBlacklistPaginationParams) -> Result<MailBlacklistPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find mail_blacklist by email
    async fn find_by_email(&self, email: &str) -> Result<Option<MailBlacklist>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if mail_blacklist exists by email
    async fn exists_by_email(&self, email: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_blacklist entities
    async fn bulk_save(&self, entities: &[MailBlacklist]) -> Result<Vec<MailBlacklist>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
