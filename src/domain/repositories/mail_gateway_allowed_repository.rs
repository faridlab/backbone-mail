//! Repository trait for MailGatewayAllowed aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailGatewayAllowed aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailGatewayAllowed;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailGatewayAllowedPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailGatewayAllowedPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailGatewayAllowedPaginatedResult {
    pub data: Vec<MailGatewayAllowed>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailGatewayAllowedFilter {
    pub fetchmail_server_id: Option<Uuid>,
    pub pattern: Option<String>,
    pub active: Option<bool>,
}

impl MailGatewayAllowedFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.fetchmail_server_id.is_some() || self.pattern.is_some() || self.active.is_some()
    }
}

/// Repository trait for MailGatewayAllowed aggregate
///
/// This trait defines the persistence contract for MailGatewayAllowed entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailGatewayAllowedRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_gateway_allowed entity
    async fn save(&self, entity: &MailGatewayAllowed) -> Result<MailGatewayAllowed>;

    /// Find mail_gateway_allowed by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailGatewayAllowed>>;

    /// Find all mail_gateway_allowed entities
    async fn find_all(&self) -> Result<Vec<MailGatewayAllowed>>;

    /// Update mail_gateway_allowed by ID
    async fn update(&self, id: &str, entity: &MailGatewayAllowed) -> Result<Option<MailGatewayAllowed>>;

    /// Delete mail_gateway_allowed by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_gateway_allowed with pagination
    async fn list(&self, params: MailGatewayAllowedPaginationParams) -> Result<MailGatewayAllowedPaginatedResult>;

    /// List mail_gateway_allowed with pagination and filters
    async fn list_with_filters(&self, params: MailGatewayAllowedPaginationParams, filters: MailGatewayAllowedFilter) -> Result<MailGatewayAllowedPaginatedResult>;

    /// Count all mail_gateway_allowed entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_gateway_allowed entities matching filters
    async fn count_with_filters(&self, filters: MailGatewayAllowedFilter) -> Result<u64>;

    /// Check if mail_gateway_allowed exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_gateway_allowed by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_gateway_allowed by ID
    async fn restore(&self, id: &str) -> Result<Option<MailGatewayAllowed>>;

    /// List soft-deleted mail_gateway_allowed entities
    async fn list_deleted(&self, params: MailGatewayAllowedPaginationParams) -> Result<MailGatewayAllowedPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_gateway_allowed entities
    async fn bulk_save(&self, entities: &[MailGatewayAllowed]) -> Result<Vec<MailGatewayAllowed>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
