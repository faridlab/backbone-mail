//! Repository trait for MailMessage aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailMessage aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{MailMessage, MailMessageType, MailModerationStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailMessagePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailMessagePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailMessagePaginatedResult {
    pub data: Vec<MailMessage>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailMessageFilter {
    pub subject: Option<String>,
    pub body: Option<String>,
    pub message_type: Option<MailMessageType>,
    pub subtype_id: Option<Uuid>,
    pub is_internal: Option<bool>,
    pub author_id: Option<Uuid>,
    pub author_guest_id: Option<Uuid>,
    pub email_from: Option<String>,
    pub message_id: Option<String>,
    pub reply_to: Option<String>,
    pub model: Option<String>,
    pub res_id: Option<Uuid>,
    pub record_name: Option<String>,
    pub moderation_status: Option<MailModerationStatus>,
    pub needaction: Option<bool>,
    pub has_error: Option<bool>,
    pub failure_reason: Option<String>,
}

impl MailMessageFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.subject.is_some() || self.body.is_some() || self.message_type.is_some() || self.subtype_id.is_some() || self.is_internal.is_some() || self.author_id.is_some() || self.author_guest_id.is_some() || self.email_from.is_some() || self.message_id.is_some() || self.reply_to.is_some() || self.model.is_some() || self.res_id.is_some() || self.record_name.is_some() || self.moderation_status.is_some() || self.needaction.is_some() || self.has_error.is_some() || self.failure_reason.is_some()
    }
}

/// Repository trait for MailMessage aggregate
///
/// This trait defines the persistence contract for MailMessage entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailMessageRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_message entity
    async fn save(&self, entity: &MailMessage) -> Result<MailMessage>;

    /// Find mail_message by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailMessage>>;

    /// Find all mail_message entities
    async fn find_all(&self) -> Result<Vec<MailMessage>>;

    /// Update mail_message by ID
    async fn update(&self, id: &str, entity: &MailMessage) -> Result<Option<MailMessage>>;

    /// Delete mail_message by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_message with pagination
    async fn list(&self, params: MailMessagePaginationParams) -> Result<MailMessagePaginatedResult>;

    /// List mail_message with pagination and filters
    async fn list_with_filters(&self, params: MailMessagePaginationParams, filters: MailMessageFilter) -> Result<MailMessagePaginatedResult>;

    /// Count all mail_message entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_message entities matching filters
    async fn count_with_filters(&self, filters: MailMessageFilter) -> Result<u64>;

    /// Check if mail_message exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_message by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_message by ID
    async fn restore(&self, id: &str) -> Result<Option<MailMessage>>;

    /// List soft-deleted mail_message entities
    async fn list_deleted(&self, params: MailMessagePaginationParams) -> Result<MailMessagePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_message entities
    async fn bulk_save(&self, entities: &[MailMessage]) -> Result<Vec<MailMessage>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
