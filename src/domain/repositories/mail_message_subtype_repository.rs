//! Repository trait for MailMessageSubtype aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailMessageSubtype aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailMessageSubtype;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailMessageSubtypePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailMessageSubtypePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailMessageSubtypePaginatedResult {
    pub data: Vec<MailMessageSubtype>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailMessageSubtypeFilter {
    pub name: Option<String>,
    pub description: Option<String>,
    pub internal: Option<bool>,
    pub parent_id: Option<Uuid>,
    pub relation_field: Option<String>,
    pub res_model: Option<String>,
    pub default: Option<bool>,
    pub hidden: Option<bool>,
    pub tracked: Option<bool>,
}

impl MailMessageSubtypeFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.description.is_some() || self.internal.is_some() || self.parent_id.is_some() || self.relation_field.is_some() || self.res_model.is_some() || self.default.is_some() || self.hidden.is_some() || self.tracked.is_some()
    }
}

/// Repository trait for MailMessageSubtype aggregate
///
/// This trait defines the persistence contract for MailMessageSubtype entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailMessageSubtypeRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_message_subtype entity
    async fn save(&self, entity: &MailMessageSubtype) -> Result<MailMessageSubtype>;

    /// Find mail_message_subtype by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailMessageSubtype>>;

    /// Find all mail_message_subtype entities
    async fn find_all(&self) -> Result<Vec<MailMessageSubtype>>;

    /// Update mail_message_subtype by ID
    async fn update(&self, id: &str, entity: &MailMessageSubtype) -> Result<Option<MailMessageSubtype>>;

    /// Delete mail_message_subtype by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_message_subtype with pagination
    async fn list(&self, params: MailMessageSubtypePaginationParams) -> Result<MailMessageSubtypePaginatedResult>;

    /// List mail_message_subtype with pagination and filters
    async fn list_with_filters(&self, params: MailMessageSubtypePaginationParams, filters: MailMessageSubtypeFilter) -> Result<MailMessageSubtypePaginatedResult>;

    /// Count all mail_message_subtype entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_message_subtype entities matching filters
    async fn count_with_filters(&self, filters: MailMessageSubtypeFilter) -> Result<u64>;

    /// Check if mail_message_subtype exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_message_subtype by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_message_subtype by ID
    async fn restore(&self, id: &str) -> Result<Option<MailMessageSubtype>>;

    /// List soft-deleted mail_message_subtype entities
    async fn list_deleted(&self, params: MailMessageSubtypePaginationParams) -> Result<MailMessageSubtypePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_message_subtype entities
    async fn bulk_save(&self, entities: &[MailMessageSubtype]) -> Result<Vec<MailMessageSubtype>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
