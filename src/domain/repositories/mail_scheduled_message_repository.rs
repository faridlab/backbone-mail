//! Repository trait for MailScheduledMessage aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailScheduledMessage aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailScheduledMessage;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailScheduledMessagePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailScheduledMessagePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailScheduledMessagePaginatedResult {
    pub data: Vec<MailScheduledMessage>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailScheduledMessageFilter {
    pub subject: Option<String>,
    pub body: Option<String>,
    pub composition_comment_option: Option<String>,
    pub model: Option<String>,
    pub res_id: Option<Uuid>,
    pub author_party_id: Option<Uuid>,
    pub is_note: Option<bool>,
    pub notification_parameters: Option<String>,
}

impl MailScheduledMessageFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.subject.is_some() || self.body.is_some() || self.composition_comment_option.is_some() || self.model.is_some() || self.res_id.is_some() || self.author_party_id.is_some() || self.is_note.is_some() || self.notification_parameters.is_some()
    }
}

/// Repository trait for MailScheduledMessage aggregate
///
/// This trait defines the persistence contract for MailScheduledMessage entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailScheduledMessageRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_scheduled_message entity
    async fn save(&self, entity: &MailScheduledMessage) -> Result<MailScheduledMessage>;

    /// Find mail_scheduled_message by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailScheduledMessage>>;

    /// Find all mail_scheduled_message entities
    async fn find_all(&self) -> Result<Vec<MailScheduledMessage>>;

    /// Update mail_scheduled_message by ID
    async fn update(&self, id: &str, entity: &MailScheduledMessage) -> Result<Option<MailScheduledMessage>>;

    /// Delete mail_scheduled_message by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_scheduled_message with pagination
    async fn list(&self, params: MailScheduledMessagePaginationParams) -> Result<MailScheduledMessagePaginatedResult>;

    /// List mail_scheduled_message with pagination and filters
    async fn list_with_filters(&self, params: MailScheduledMessagePaginationParams, filters: MailScheduledMessageFilter) -> Result<MailScheduledMessagePaginatedResult>;

    /// Count all mail_scheduled_message entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_scheduled_message entities matching filters
    async fn count_with_filters(&self, filters: MailScheduledMessageFilter) -> Result<u64>;

    /// Check if mail_scheduled_message exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_scheduled_message by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_scheduled_message by ID
    async fn restore(&self, id: &str) -> Result<Option<MailScheduledMessage>>;

    /// List soft-deleted mail_scheduled_message entities
    async fn list_deleted(&self, params: MailScheduledMessagePaginationParams) -> Result<MailScheduledMessagePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_scheduled_message entities
    async fn bulk_save(&self, entities: &[MailScheduledMessage]) -> Result<Vec<MailScheduledMessage>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
