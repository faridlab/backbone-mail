//! Repository trait for MailFollowers aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailFollowers aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailFollowers;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailFollowersPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailFollowersPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailFollowersPaginatedResult {
    pub data: Vec<MailFollowers>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailFollowersFilter {
    pub res_model: Option<String>,
    pub res_id: Option<Uuid>,
    pub partner_id: Option<Uuid>,
    pub channel_id: Option<Uuid>,
}

impl MailFollowersFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.res_model.is_some() || self.res_id.is_some() || self.partner_id.is_some() || self.channel_id.is_some()
    }
}

/// Repository trait for MailFollowers aggregate
///
/// This trait defines the persistence contract for MailFollowers entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailFollowersRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_followers entity
    async fn save(&self, entity: &MailFollowers) -> Result<MailFollowers>;

    /// Find mail_followers by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailFollowers>>;

    /// Find all mail_followers entities
    async fn find_all(&self) -> Result<Vec<MailFollowers>>;

    /// Update mail_followers by ID
    async fn update(&self, id: &str, entity: &MailFollowers) -> Result<Option<MailFollowers>>;

    /// Delete mail_followers by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_followers with pagination
    async fn list(&self, params: MailFollowersPaginationParams) -> Result<MailFollowersPaginatedResult>;

    /// List mail_followers with pagination and filters
    async fn list_with_filters(&self, params: MailFollowersPaginationParams, filters: MailFollowersFilter) -> Result<MailFollowersPaginatedResult>;

    /// Count all mail_followers entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_followers entities matching filters
    async fn count_with_filters(&self, filters: MailFollowersFilter) -> Result<u64>;

    /// Check if mail_followers exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_followers by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_followers by ID
    async fn restore(&self, id: &str) -> Result<Option<MailFollowers>>;

    /// List soft-deleted mail_followers entities
    async fn list_deleted(&self, params: MailFollowersPaginationParams) -> Result<MailFollowersPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_followers entities
    async fn bulk_save(&self, entities: &[MailFollowers]) -> Result<Vec<MailFollowers>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
