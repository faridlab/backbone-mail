//! Repository trait for MailMessageAttachment aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailMessageAttachment aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailMessageAttachment;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailMessageAttachmentPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailMessageAttachmentPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailMessageAttachmentPaginatedResult {
    pub data: Vec<MailMessageAttachment>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailMessageAttachmentFilter {
    pub message_id: Option<Uuid>,
    pub attachment_id: Option<Uuid>,
}

impl MailMessageAttachmentFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.message_id.is_some() || self.attachment_id.is_some()
    }
}

/// Repository trait for MailMessageAttachment aggregate
///
/// This trait defines the persistence contract for MailMessageAttachment entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailMessageAttachmentRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_message_attachment entity
    async fn save(&self, entity: &MailMessageAttachment) -> Result<MailMessageAttachment>;

    /// Find mail_message_attachment by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailMessageAttachment>>;

    /// Find all mail_message_attachment entities
    async fn find_all(&self) -> Result<Vec<MailMessageAttachment>>;

    /// Update mail_message_attachment by ID
    async fn update(&self, id: &str, entity: &MailMessageAttachment) -> Result<Option<MailMessageAttachment>>;

    /// Delete mail_message_attachment by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_message_attachment with pagination
    async fn list(&self, params: MailMessageAttachmentPaginationParams) -> Result<MailMessageAttachmentPaginatedResult>;

    /// List mail_message_attachment with pagination and filters
    async fn list_with_filters(&self, params: MailMessageAttachmentPaginationParams, filters: MailMessageAttachmentFilter) -> Result<MailMessageAttachmentPaginatedResult>;

    /// Count all mail_message_attachment entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_message_attachment entities matching filters
    async fn count_with_filters(&self, filters: MailMessageAttachmentFilter) -> Result<u64>;

    /// Check if mail_message_attachment exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_message_attachment by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_message_attachment by ID
    async fn restore(&self, id: &str) -> Result<Option<MailMessageAttachment>>;

    /// List soft-deleted mail_message_attachment entities
    async fn list_deleted(&self, params: MailMessageAttachmentPaginationParams) -> Result<MailMessageAttachmentPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_message_attachment entities
    async fn bulk_save(&self, entities: &[MailMessageAttachment]) -> Result<Vec<MailMessageAttachment>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
