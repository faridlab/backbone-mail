//! Repository trait for MailGuest aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailGuest aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailGuest;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailGuestPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailGuestPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailGuestPaginatedResult {
    pub data: Vec<MailGuest>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailGuestFilter {
    pub name: Option<String>,
    pub country_id: Option<Uuid>,
    pub active: Option<bool>,
}

impl MailGuestFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.country_id.is_some() || self.active.is_some()
    }
}

/// Repository trait for MailGuest aggregate
///
/// This trait defines the persistence contract for MailGuest entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailGuestRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_guest entity
    async fn save(&self, entity: &MailGuest) -> Result<MailGuest>;

    /// Find mail_guest by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailGuest>>;

    /// Find all mail_guest entities
    async fn find_all(&self) -> Result<Vec<MailGuest>>;

    /// Update mail_guest by ID
    async fn update(&self, id: &str, entity: &MailGuest) -> Result<Option<MailGuest>>;

    /// Delete mail_guest by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_guest with pagination
    async fn list(&self, params: MailGuestPaginationParams) -> Result<MailGuestPaginatedResult>;

    /// List mail_guest with pagination and filters
    async fn list_with_filters(&self, params: MailGuestPaginationParams, filters: MailGuestFilter) -> Result<MailGuestPaginatedResult>;

    /// Count all mail_guest entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_guest entities matching filters
    async fn count_with_filters(&self, filters: MailGuestFilter) -> Result<u64>;

    /// Check if mail_guest exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_guest by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_guest by ID
    async fn restore(&self, id: &str) -> Result<Option<MailGuest>>;

    /// List soft-deleted mail_guest entities
    async fn list_deleted(&self, params: MailGuestPaginationParams) -> Result<MailGuestPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_guest entities
    async fn bulk_save(&self, entities: &[MailGuest]) -> Result<Vec<MailGuest>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
