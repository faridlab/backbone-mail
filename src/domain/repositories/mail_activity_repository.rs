//! Repository trait for MailActivity aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailActivity aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{MailActivity, MailActivityState};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailActivityPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailActivityPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailActivityPaginatedResult {
    pub data: Vec<MailActivity>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailActivityFilter {
    pub res_model: Option<String>,
    pub res_id: Option<Uuid>,
    pub res_model_id: Option<Uuid>,
    pub activity_type_id: Option<Uuid>,
    pub summary: Option<String>,
    pub note: Option<String>,
    pub user_id: Option<Uuid>,
    pub requested_user_id: Option<Uuid>,
    pub state: Option<MailActivityState>,
    pub active: Option<bool>,
    pub has_recommended_activities: Option<bool>,
    pub calendar_event_id: Option<Uuid>,
}

impl MailActivityFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.res_model.is_some() || self.res_id.is_some() || self.res_model_id.is_some() || self.activity_type_id.is_some() || self.summary.is_some() || self.note.is_some() || self.user_id.is_some() || self.requested_user_id.is_some() || self.state.is_some() || self.active.is_some() || self.has_recommended_activities.is_some() || self.calendar_event_id.is_some()
    }
}

/// Repository trait for MailActivity aggregate
///
/// This trait defines the persistence contract for MailActivity entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailActivityRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_activity entity
    async fn save(&self, entity: &MailActivity) -> Result<MailActivity>;

    /// Find mail_activity by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailActivity>>;

    /// Find all mail_activity entities
    async fn find_all(&self) -> Result<Vec<MailActivity>>;

    /// Update mail_activity by ID
    async fn update(&self, id: &str, entity: &MailActivity) -> Result<Option<MailActivity>>;

    /// Delete mail_activity by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_activity with pagination
    async fn list(&self, params: MailActivityPaginationParams) -> Result<MailActivityPaginatedResult>;

    /// List mail_activity with pagination and filters
    async fn list_with_filters(&self, params: MailActivityPaginationParams, filters: MailActivityFilter) -> Result<MailActivityPaginatedResult>;

    /// Count all mail_activity entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_activity entities matching filters
    async fn count_with_filters(&self, filters: MailActivityFilter) -> Result<u64>;

    /// Check if mail_activity exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_activity by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_activity by ID
    async fn restore(&self, id: &str) -> Result<Option<MailActivity>>;

    /// List soft-deleted mail_activity entities
    async fn list_deleted(&self, params: MailActivityPaginationParams) -> Result<MailActivityPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_activity entities
    async fn bulk_save(&self, entities: &[MailActivity]) -> Result<Vec<MailActivity>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
