//! Repository trait for SmsTemplate aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SmsTemplate aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::SmsTemplate;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SmsTemplatePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl SmsTemplatePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct SmsTemplatePaginatedResult {
    pub data: Vec<SmsTemplate>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SmsTemplateFilter {
    pub name: Option<String>,
    pub body: Option<String>,
    pub model_id: Option<Uuid>,
    pub lang: Option<String>,
    pub condition: Option<String>,
    pub active: Option<bool>,
}

impl SmsTemplateFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.body.is_some() || self.model_id.is_some() || self.lang.is_some() || self.condition.is_some() || self.active.is_some()
    }
}

/// Repository trait for SmsTemplate aggregate
///
/// This trait defines the persistence contract for SmsTemplate entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SmsTemplateRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new sms_template entity
    async fn save(&self, entity: &SmsTemplate) -> Result<SmsTemplate>;

    /// Find sms_template by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SmsTemplate>>;

    /// Find all sms_template entities
    async fn find_all(&self) -> Result<Vec<SmsTemplate>>;

    /// Update sms_template by ID
    async fn update(&self, id: &str, entity: &SmsTemplate) -> Result<Option<SmsTemplate>>;

    /// Delete sms_template by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List sms_template with pagination
    async fn list(&self, params: SmsTemplatePaginationParams) -> Result<SmsTemplatePaginatedResult>;

    /// List sms_template with pagination and filters
    async fn list_with_filters(&self, params: SmsTemplatePaginationParams, filters: SmsTemplateFilter) -> Result<SmsTemplatePaginatedResult>;

    /// Count all sms_template entities
    async fn count(&self) -> Result<u64>;

    /// Count sms_template entities matching filters
    async fn count_with_filters(&self, filters: SmsTemplateFilter) -> Result<u64>;

    /// Check if sms_template exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete sms_template by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted sms_template by ID
    async fn restore(&self, id: &str) -> Result<Option<SmsTemplate>>;

    /// List soft-deleted sms_template entities
    async fn list_deleted(&self, params: SmsTemplatePaginationParams) -> Result<SmsTemplatePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save sms_template entities
    async fn bulk_save(&self, entities: &[SmsTemplate]) -> Result<Vec<SmsTemplate>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
