//! Repository trait for MailPresence aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailPresence aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{MailPresence, MailPresenceStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailPresencePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailPresencePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailPresencePaginatedResult {
    pub data: Vec<MailPresence>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailPresenceFilter {
    pub user_id: Option<Uuid>,
    pub guest_id: Option<Uuid>,
    pub status: Option<MailPresenceStatus>,
}

impl MailPresenceFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.guest_id.is_some() || self.status.is_some()
    }
}

/// Repository trait for MailPresence aggregate
///
/// This trait defines the persistence contract for MailPresence entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailPresenceRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_presence entity
    async fn save(&self, entity: &MailPresence) -> Result<MailPresence>;

    /// Find mail_presence by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailPresence>>;

    /// Find all mail_presence entities
    async fn find_all(&self) -> Result<Vec<MailPresence>>;

    /// Update mail_presence by ID
    async fn update(&self, id: &str, entity: &MailPresence) -> Result<Option<MailPresence>>;

    /// Delete mail_presence by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_presence with pagination
    async fn list(&self, params: MailPresencePaginationParams) -> Result<MailPresencePaginatedResult>;

    /// List mail_presence with pagination and filters
    async fn list_with_filters(&self, params: MailPresencePaginationParams, filters: MailPresenceFilter) -> Result<MailPresencePaginatedResult>;

    /// Count all mail_presence entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_presence entities matching filters
    async fn count_with_filters(&self, filters: MailPresenceFilter) -> Result<u64>;

    /// Check if mail_presence exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_presence by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_presence by ID
    async fn restore(&self, id: &str) -> Result<Option<MailPresence>>;

    /// List soft-deleted mail_presence entities
    async fn list_deleted(&self, params: MailPresencePaginationParams) -> Result<MailPresencePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_presence entities
    async fn bulk_save(&self, entities: &[MailPresence]) -> Result<Vec<MailPresence>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
