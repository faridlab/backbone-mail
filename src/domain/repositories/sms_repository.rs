//! Repository trait for Sms aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Sms aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Sms, SmsFailureType, SmsState};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SmsPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl SmsPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct SmsPaginatedResult {
    pub data: Vec<Sms>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SmsFilter {
    pub uuid: Option<String>,
    pub number: Option<String>,
    pub body: Option<String>,
    pub state: Option<SmsState>,
    pub failure_type: Option<SmsFailureType>,
    pub error_message: Option<String>,
    pub mail_message_id: Option<Uuid>,
}

impl SmsFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.uuid.is_some() || self.number.is_some() || self.body.is_some() || self.state.is_some() || self.failure_type.is_some() || self.error_message.is_some() || self.mail_message_id.is_some()
    }
}

/// Repository trait for Sms aggregate
///
/// This trait defines the persistence contract for Sms entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SmsRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new sms entity
    async fn save(&self, entity: &Sms) -> Result<Sms>;

    /// Find sms by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Sms>>;

    /// Find all sms entities
    async fn find_all(&self) -> Result<Vec<Sms>>;

    /// Update sms by ID
    async fn update(&self, id: &str, entity: &Sms) -> Result<Option<Sms>>;

    /// Delete sms by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List sms with pagination
    async fn list(&self, params: SmsPaginationParams) -> Result<SmsPaginatedResult>;

    /// List sms with pagination and filters
    async fn list_with_filters(&self, params: SmsPaginationParams, filters: SmsFilter) -> Result<SmsPaginatedResult>;

    /// Count all sms entities
    async fn count(&self) -> Result<u64>;

    /// Count sms entities matching filters
    async fn count_with_filters(&self, filters: SmsFilter) -> Result<u64>;

    /// Check if sms exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete sms by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted sms by ID
    async fn restore(&self, id: &str) -> Result<Option<Sms>>;

    /// List soft-deleted sms entities
    async fn list_deleted(&self, params: SmsPaginationParams) -> Result<SmsPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find sms by uuid
    async fn find_by_uuid(&self, uuid: &str) -> Result<Option<Sms>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if sms exists by uuid
    async fn exists_by_uuid(&self, uuid: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save sms entities
    async fn bulk_save(&self, entities: &[Sms]) -> Result<Vec<Sms>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
