//! Repository trait for MailAliasDomain aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailAliasDomain aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailAliasDomain;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailAliasDomainPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailAliasDomainPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailAliasDomainPaginatedResult {
    pub data: Vec<MailAliasDomain>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailAliasDomainFilter {
    pub name: Option<String>,
    pub catchall_alias: Option<String>,
    pub bounce_alias: Option<String>,
    pub bounce_alias_email: Option<String>,
    pub catchall_alias_email: Option<String>,
    pub use_mx: Option<bool>,
    pub mail_server_id: Option<Uuid>,
    pub from_filter: Option<String>,
}

impl MailAliasDomainFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.catchall_alias.is_some() || self.bounce_alias.is_some() || self.bounce_alias_email.is_some() || self.catchall_alias_email.is_some() || self.use_mx.is_some() || self.mail_server_id.is_some() || self.from_filter.is_some()
    }
}

/// Repository trait for MailAliasDomain aggregate
///
/// This trait defines the persistence contract for MailAliasDomain entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailAliasDomainRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_alias_domain entity
    async fn save(&self, entity: &MailAliasDomain) -> Result<MailAliasDomain>;

    /// Find mail_alias_domain by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailAliasDomain>>;

    /// Find all mail_alias_domain entities
    async fn find_all(&self) -> Result<Vec<MailAliasDomain>>;

    /// Update mail_alias_domain by ID
    async fn update(&self, id: &str, entity: &MailAliasDomain) -> Result<Option<MailAliasDomain>>;

    /// Delete mail_alias_domain by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_alias_domain with pagination
    async fn list(&self, params: MailAliasDomainPaginationParams) -> Result<MailAliasDomainPaginatedResult>;

    /// List mail_alias_domain with pagination and filters
    async fn list_with_filters(&self, params: MailAliasDomainPaginationParams, filters: MailAliasDomainFilter) -> Result<MailAliasDomainPaginatedResult>;

    /// Count all mail_alias_domain entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_alias_domain entities matching filters
    async fn count_with_filters(&self, filters: MailAliasDomainFilter) -> Result<u64>;

    /// Check if mail_alias_domain exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_alias_domain by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_alias_domain by ID
    async fn restore(&self, id: &str) -> Result<Option<MailAliasDomain>>;

    /// List soft-deleted mail_alias_domain entities
    async fn list_deleted(&self, params: MailAliasDomainPaginationParams) -> Result<MailAliasDomainPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_alias_domain entities
    async fn bulk_save(&self, entities: &[MailAliasDomain]) -> Result<Vec<MailAliasDomain>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
