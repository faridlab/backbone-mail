//! Repository trait for SmsTracker aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SmsTracker aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{SmsTracker, MailNotificationStatus, NotificationFailureType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SmsTrackerPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl SmsTrackerPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct SmsTrackerPaginatedResult {
    pub data: Vec<SmsTracker>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SmsTrackerFilter {
    pub sms_uuid: Option<String>,
    pub message_id: Option<Uuid>,
    pub notification_id: Option<Uuid>,
    pub state: Option<MailNotificationStatus>,
    pub failure_type: Option<NotificationFailureType>,
    pub failure_reason: Option<String>,
    pub recipient: Option<String>,
}

impl SmsTrackerFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.sms_uuid.is_some() || self.message_id.is_some() || self.notification_id.is_some() || self.state.is_some() || self.failure_type.is_some() || self.failure_reason.is_some() || self.recipient.is_some()
    }
}

/// Repository trait for SmsTracker aggregate
///
/// This trait defines the persistence contract for SmsTracker entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SmsTrackerRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new sms_tracker entity
    async fn save(&self, entity: &SmsTracker) -> Result<SmsTracker>;

    /// Find sms_tracker by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SmsTracker>>;

    /// Find all sms_tracker entities
    async fn find_all(&self) -> Result<Vec<SmsTracker>>;

    /// Update sms_tracker by ID
    async fn update(&self, id: &str, entity: &SmsTracker) -> Result<Option<SmsTracker>>;

    /// Delete sms_tracker by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List sms_tracker with pagination
    async fn list(&self, params: SmsTrackerPaginationParams) -> Result<SmsTrackerPaginatedResult>;

    /// List sms_tracker with pagination and filters
    async fn list_with_filters(&self, params: SmsTrackerPaginationParams, filters: SmsTrackerFilter) -> Result<SmsTrackerPaginatedResult>;

    /// Count all sms_tracker entities
    async fn count(&self) -> Result<u64>;

    /// Count sms_tracker entities matching filters
    async fn count_with_filters(&self, filters: SmsTrackerFilter) -> Result<u64>;

    /// Check if sms_tracker exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete sms_tracker by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted sms_tracker by ID
    async fn restore(&self, id: &str) -> Result<Option<SmsTracker>>;

    /// List soft-deleted sms_tracker entities
    async fn list_deleted(&self, params: SmsTrackerPaginationParams) -> Result<SmsTrackerPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find sms_tracker by sms_uuid
    async fn find_by_sms_uuid(&self, sms_uuid: &str) -> Result<Option<SmsTracker>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if sms_tracker exists by sms_uuid
    async fn exists_by_sms_uuid(&self, sms_uuid: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save sms_tracker entities
    async fn bulk_save(&self, entities: &[SmsTracker]) -> Result<Vec<SmsTracker>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
