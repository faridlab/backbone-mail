//! Repository trait for MailAlias aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailAlias aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{MailAlias, MailAliasContact};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailAliasPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailAliasPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailAliasPaginatedResult {
    pub data: Vec<MailAlias>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailAliasFilter {
    pub alias_name: Option<String>,
    pub alias_domain_id: Option<Uuid>,
    pub alias_contact: Option<MailAliasContact>,
    pub alias_model_id: Option<Uuid>,
    pub alias_parent_model_id: Option<Uuid>,
    pub alias_parent_thread_id: Option<Uuid>,
    pub alias_user_id: Option<Uuid>,
    pub alias_defaults: Option<String>,
    pub alias_force_thread_id: Option<Uuid>,
    pub alias_reply_to_address: Option<String>,
}

impl MailAliasFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.alias_name.is_some() || self.alias_domain_id.is_some() || self.alias_contact.is_some() || self.alias_model_id.is_some() || self.alias_parent_model_id.is_some() || self.alias_parent_thread_id.is_some() || self.alias_user_id.is_some() || self.alias_defaults.is_some() || self.alias_force_thread_id.is_some() || self.alias_reply_to_address.is_some()
    }
}

/// Repository trait for MailAlias aggregate
///
/// This trait defines the persistence contract for MailAlias entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailAliasRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_alias entity
    async fn save(&self, entity: &MailAlias) -> Result<MailAlias>;

    /// Find mail_alias by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailAlias>>;

    /// Find all mail_alias entities
    async fn find_all(&self) -> Result<Vec<MailAlias>>;

    /// Update mail_alias by ID
    async fn update(&self, id: &str, entity: &MailAlias) -> Result<Option<MailAlias>>;

    /// Delete mail_alias by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_alias with pagination
    async fn list(&self, params: MailAliasPaginationParams) -> Result<MailAliasPaginatedResult>;

    /// List mail_alias with pagination and filters
    async fn list_with_filters(&self, params: MailAliasPaginationParams, filters: MailAliasFilter) -> Result<MailAliasPaginatedResult>;

    /// Count all mail_alias entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_alias entities matching filters
    async fn count_with_filters(&self, filters: MailAliasFilter) -> Result<u64>;

    /// Check if mail_alias exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_alias by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_alias by ID
    async fn restore(&self, id: &str) -> Result<Option<MailAlias>>;

    /// List soft-deleted mail_alias entities
    async fn list_deleted(&self, params: MailAliasPaginationParams) -> Result<MailAliasPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_alias entities
    async fn bulk_save(&self, entities: &[MailAlias]) -> Result<Vec<MailAlias>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
