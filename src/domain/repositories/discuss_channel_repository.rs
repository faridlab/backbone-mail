//! Repository trait for DiscussChannel aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the DiscussChannel aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{DiscussChannel, DiscussChannelType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DiscussChannelPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl DiscussChannelPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct DiscussChannelPaginatedResult {
    pub data: Vec<DiscussChannel>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DiscussChannelFilter {
    pub name: Option<String>,
    pub channel_type: Option<DiscussChannelType>,
    pub description: Option<String>,
    pub uuid: Option<String>,
    pub default_access_mode: Option<String>,
    pub avatar_128: Option<String>,
    pub email_send: Option<bool>,
    pub moderation: Option<bool>,
    pub group_public_id: Option<Uuid>,
    pub alias_id: Option<Uuid>,
    pub last_message_id: Option<Uuid>,
}

impl DiscussChannelFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.channel_type.is_some() || self.description.is_some() || self.uuid.is_some() || self.default_access_mode.is_some() || self.avatar_128.is_some() || self.email_send.is_some() || self.moderation.is_some() || self.group_public_id.is_some() || self.alias_id.is_some() || self.last_message_id.is_some()
    }
}

/// Repository trait for DiscussChannel aggregate
///
/// This trait defines the persistence contract for DiscussChannel entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait DiscussChannelRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new discuss_channel entity
    async fn save(&self, entity: &DiscussChannel) -> Result<DiscussChannel>;

    /// Find discuss_channel by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<DiscussChannel>>;

    /// Find all discuss_channel entities
    async fn find_all(&self) -> Result<Vec<DiscussChannel>>;

    /// Update discuss_channel by ID
    async fn update(&self, id: &str, entity: &DiscussChannel) -> Result<Option<DiscussChannel>>;

    /// Delete discuss_channel by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List discuss_channel with pagination
    async fn list(&self, params: DiscussChannelPaginationParams) -> Result<DiscussChannelPaginatedResult>;

    /// List discuss_channel with pagination and filters
    async fn list_with_filters(&self, params: DiscussChannelPaginationParams, filters: DiscussChannelFilter) -> Result<DiscussChannelPaginatedResult>;

    /// Count all discuss_channel entities
    async fn count(&self) -> Result<u64>;

    /// Count discuss_channel entities matching filters
    async fn count_with_filters(&self, filters: DiscussChannelFilter) -> Result<u64>;

    /// Check if discuss_channel exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete discuss_channel by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted discuss_channel by ID
    async fn restore(&self, id: &str) -> Result<Option<DiscussChannel>>;

    /// List soft-deleted discuss_channel entities
    async fn list_deleted(&self, params: DiscussChannelPaginationParams) -> Result<DiscussChannelPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find discuss_channel by uuid
    async fn find_by_uuid(&self, uuid: Option<&str>) -> Result<Option<DiscussChannel>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if discuss_channel exists by uuid
    async fn exists_by_uuid(&self, uuid: Option<&str>) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save discuss_channel entities
    async fn bulk_save(&self, entities: &[DiscussChannel]) -> Result<Vec<DiscussChannel>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
