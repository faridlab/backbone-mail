//! Domain events for MailMessage entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MailMessage;

/// Domain events for MailMessage — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MailMessage>)` variant.
pub type MailMessageEvent = CrudEvent<MailMessage>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MailMessage events.
pub type MailMessageEventPublisher = Arc<dyn CrudEventPublisher<MailMessage> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMailMessageEventPublisher = NoOpCrudEventPublisher<MailMessage>;
