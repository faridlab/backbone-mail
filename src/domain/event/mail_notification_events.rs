//! Domain events for MailNotification entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MailNotification;

/// Domain events for MailNotification — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MailNotification>)` variant.
pub type MailNotificationEvent = CrudEvent<MailNotification>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MailNotification events.
pub type MailNotificationEventPublisher = Arc<dyn CrudEventPublisher<MailNotification> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMailNotificationEventPublisher = NoOpCrudEventPublisher<MailNotification>;
