//! Domain events for MailMessageStar entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MailMessageStar;

/// Domain events for MailMessageStar — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MailMessageStar>)` variant.
pub type MailMessageStarEvent = CrudEvent<MailMessageStar>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MailMessageStar events.
pub type MailMessageStarEventPublisher = Arc<dyn CrudEventPublisher<MailMessageStar> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMailMessageStarEventPublisher = NoOpCrudEventPublisher<MailMessageStar>;
