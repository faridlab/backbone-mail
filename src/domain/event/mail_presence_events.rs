//! Domain events for MailPresence entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MailPresence;

/// Domain events for MailPresence — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MailPresence>)` variant.
pub type MailPresenceEvent = CrudEvent<MailPresence>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MailPresence events.
pub type MailPresenceEventPublisher = Arc<dyn CrudEventPublisher<MailPresence> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMailPresenceEventPublisher = NoOpCrudEventPublisher<MailPresence>;
