//! Domain events for MailServer entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MailServer;

/// Domain events for MailServer — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MailServer>)` variant.
pub type MailServerEvent = CrudEvent<MailServer>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MailServer events.
pub type MailServerEventPublisher = Arc<dyn CrudEventPublisher<MailServer> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMailServerEventPublisher = NoOpCrudEventPublisher<MailServer>;
