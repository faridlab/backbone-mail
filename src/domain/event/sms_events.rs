//! Domain events for Sms entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Sms;

/// Domain events for Sms — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Sms>)` variant.
pub type SmsEvent = CrudEvent<Sms>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Sms events.
pub type SmsEventPublisher = Arc<dyn CrudEventPublisher<Sms> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopSmsEventPublisher = NoOpCrudEventPublisher<Sms>;
