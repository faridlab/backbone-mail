//! Domain events for DiscussChannel entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::DiscussChannel;

/// Domain events for DiscussChannel — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<DiscussChannel>)` variant.
pub type DiscussChannelEvent = CrudEvent<DiscussChannel>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for DiscussChannel events.
pub type DiscussChannelEventPublisher = Arc<dyn CrudEventPublisher<DiscussChannel> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopDiscussChannelEventPublisher = NoOpCrudEventPublisher<DiscussChannel>;
