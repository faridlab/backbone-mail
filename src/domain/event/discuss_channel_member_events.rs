//! Domain events for DiscussChannelMember entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::DiscussChannelMember;

/// Domain events for DiscussChannelMember — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<DiscussChannelMember>)` variant.
pub type DiscussChannelMemberEvent = CrudEvent<DiscussChannelMember>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for DiscussChannelMember events.
pub type DiscussChannelMemberEventPublisher = Arc<dyn CrudEventPublisher<DiscussChannelMember> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopDiscussChannelMemberEventPublisher = NoOpCrudEventPublisher<DiscussChannelMember>;
