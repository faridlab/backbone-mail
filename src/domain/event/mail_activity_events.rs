//! Domain events for MailActivity entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MailActivity;

/// Domain events for MailActivity — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MailActivity>)` variant.
pub type MailActivityEvent = CrudEvent<MailActivity>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MailActivity events.
pub type MailActivityEventPublisher = Arc<dyn CrudEventPublisher<MailActivity> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMailActivityEventPublisher = NoOpCrudEventPublisher<MailActivity>;
