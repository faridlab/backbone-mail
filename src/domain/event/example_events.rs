//! Domain events for Example entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Example;

/// Domain events for Example — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Example>)` variant.
pub type ExampleEvent = CrudEvent<Example>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Example events.
pub type ExampleEventPublisher = Arc<dyn CrudEventPublisher<Example> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopExampleEventPublisher = NoOpCrudEventPublisher<Example>;
