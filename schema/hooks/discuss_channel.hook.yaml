# =============================================================================
# DiscussChannelHooks — discuss.channel (M37).
#
# NO state machine: channel_type is set at create and IMMUTABLE after (Odoo
# raises on any write that changes it — the kind drives member semantics,
# default name and UI surface, and a silent rewrite would corrupt
# chat-vs-channel behavior). A "machine" with zero transitions is not a
# machine — the generator cannot compile one, and the DSL shouldn't model
# immutability as an all-final lattice. Immutability is enforced at the
# write layer (port-notes M37-IMMUTABLE; no channel write service exists
# in increment 1 — discuss is tables + guards only).
# =============================================================================

name: DiscussChannelHooks
model: DiscussChannel
