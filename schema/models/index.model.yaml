# =============================================================================
# mail Module - Schema Index
# =============================================================================
# Version: 2.0
# Description: Module schema index. `module:` and `schema:` are stamped from the
# module name by `metaphor module create`. `schema:` gives this module its OWN
# Postgres schema (migrations emit `CREATE SCHEMA mail` and qualify tables
# as `mail.<table>`) — the convention every backbone module follows
# (see backbone-sapiens, backbone-bucket). Do not remove it.
# =============================================================================

module: messaging
version: 1
schema: messaging
description: "Odoo mail core — message/notification/followers/activity/alias/sms queue (single-tenant, fence none)"

company_fence: none

# ADR-0014 posture 4: the Odoo messaging stack is single-tenant — no company
# dimension anywhere (theme C2: zero ir.rules, no company_id). Declaring
# `none` emits no RLS migration; synthesizing company columns would change
# behavior, not just enforcement. Dir/cargo say `backbone-mail` (the framework
# crate `backbone-messaging` took that name); schema keys stay `messaging`.

config:
  database: postgresql
  soft_delete: true
  audit: true
  default_timestamps: true
  generators:
    disabled:
      - graphql
      - grpc
      - proto

# Reuse sapiens for user identity (actors) — logical refs, no DB constraint.
external_imports:
  - module: sapiens
    types: [User]

# Shared value-object types available to all models in this module.
shared_types:
  Timestamps:
    created_at:
      type: datetime
      attributes: ["@default(now)"]
      description: "Record creation timestamp"
    updated_at:
      type: datetime
      attributes: ["@updated_at"]
      description: "Last update timestamp"
    deleted_at:
      type: datetime?
      description: "Soft delete timestamp"

  Actors:
    created_by:
      type: uuid?
      attributes: ["@foreign_key(sapiens.User.id)"]
      description: "User who created this record"
    updated_by:
      type: uuid?
      attributes: ["@foreign_key(sapiens.User.id)"]
      description: "User who last updated this record"
    deleted_by:
      type: uuid?
      attributes: ["@foreign_key(sapiens.User.id)"]
      description: "User who deleted this record"

  Metadata: [Timestamps, Actors]

# Import each entity model (informational — discovery is a recursive glob,
# but keeping the list accurate helps readers).
imports:
  - core.model.yaml
  - activity.model.yaml
  - alias.model.yaml
  - discuss.model.yaml
  - schedule.model.yaml
  - attachment.model.yaml
  - sms.model.yaml
  - phone-validation.model.yaml

# =============================================================================
# FLAG-ID COVERAGE — docs/odoo/messaging port (this module closes):
#
# Mail core (docs/odoo/messaging/mail):
#   MAIL-M1  MailMessage               core.model.yaml
#   MAIL-M2  Mail                      core.model.yaml (+hooks/mail.hook.yaml)
#   MAIL-M3  MailNotification          core.model.yaml (+hooks/mail_notification.hook.yaml, label inversion)
#   M4       MailMessageSubtype        core.model.yaml
#   M5       MailMessageReaction       core.model.yaml
#   M10      MailFollowers             core.model.yaml (+hooks/mail_followers.hook.yaml, G-MAIL-1)
#   M11      MailPresence              core.model.yaml (+hooks/mail_presence.hook.yaml)
#   M16      polymorphic edge          message (model,res_id) columns — the edge lives HERE, hosts get no column
#   M22      MailTrackingValue         core.model.yaml
#   M24      MailBlacklist             core.model.yaml (+hooks/mail_blacklist.hook.yaml, G-MAIL-2)
#   M33      MailAlias                 alias.model.yaml
#   M34      MailAliasDomain           alias.model.yaml (+hooks/mail_alias_domain.hook.yaml, G-MAIL-5)
#   M37      DiscussChannel            discuss.model.yaml (+hooks/discuss_channel.hook.yaml)
#   M38      DiscussChannelMember      discuss.model.yaml (+hooks/discuss_channel_member.hook.yaml, G-MAIL-8)
#   M43      MailGuest                 discuss.model.yaml
#   MAIL-M17 MailActivity              activity.model.yaml (state = projection)
#   M19      MailActivityType          activity.model.yaml (+hooks/mail_activity_type.hook.yaml)
#   M20      MailActivityPlan          activity.model.yaml
#   M21      MailActivityPlanTemplate  activity.model.yaml
#   G-MAIL-3/4 activity CHECKs         hooks/mail_activity.hook.yaml
#   MAIL-B6  inbound message_id dedup  core.model.yaml (message_id indexed; advisory-lock dedup is gateway-layer, Increment 2+)
#   MAIL-B8  notification GC 180d      hooks/index.hook.yaml (mail-notification-gc job; portal-row carve-out NOT ported — a port should reap them)
#   MAIL-M37.1 pinned messages         core.model.yaml MailMessage.pinned_at (source-verified: Odoo pins via a column
#                                      on mail.message, NOT a join table; raw UPDATE deliberately skips updated_at — see port-notes)
#   MAIL-M38.1 member read state       discuss.model.yaml (fetched_message_id, new_message_separator [int→uuid adaptation],
#                                      custom_notifications, mute_until_dt ['+infinity' = until-unmuted], unpin_dt, last_seen_dt)
#
# SMS (docs/odoo/messaging/sms):
#   SM-M1/2  Sms (queue row)           sms.model.yaml (+hooks/sms.hook.yaml machine, G-SM1)
#   SM-M3    SmsTemplate               sms.model.yaml
#   SM-M1/4  SmsTracker                sms.model.yaml (+hooks/sms_tracker.hook.yaml, G-SM2, SM-B6 trigger intent)
#   SM-M5    sms overlay folded        core.model.yaml (message_type/notification_type 'sms', NotificationFailureType vocab)
#   SM-F19   label inversion           Sms.state + MailNotification.notification_status display_labels (pending='Sent', sent='Delivered')
#   SM-B13   stuck-process sweep       hooks/index.hook.yaml (sms-stuck-process-sweep job — Increment 2)
#
# SCHEDULED MESSAGES + ATTACHMENTS (Increment 2):
#   MAIL-M8  MailMessageSchedule       schedule.model.yaml (notify-later; delete-on-dispatch, NO state column — SKIP LOCKED claim)
#   MAIL-M9  MailScheduledMessage      schedule.model.yaml (post-later composer values; posts via message_post as creator)
#   MAIL-M45 MailAttachment (slim)     attachment.model.yaml (full ir.attachment port stays with `system`)
#            MailMessageAttachment     attachment.model.yaml (message↔attachment join, unique)
#            MailMessageStar           attachment.model.yaml (starred m2m materialized, unique partner+message)
#
# PHONE VALIDATION (docs/odoo/marketing/mass_mailing_sms opener; the addon
# is Odoo's phone_validation — E.164 sanitize + phone.blacklist):
#   phone.blacklist             phone-validation.model.yaml (GLOBAL consent list, fence none by declaration;
#                              number stored E.164-canonical — formatter-gated writes close the email-blacklist
#                              near-duplicate class; active-archive pattern, no reason column)
#   MailBlacklist.opt_out_reason_id  core.model.yaml (nullable logical-uuid ref to the mailing module's
#                              OptOutReason catalog — opaque here, the inventory service_project_id class)
#                              The E164 formatter (phone_format / sanitize_candidates) and the
#                              PhoneBookPort are hand-written services, not schema artifacts.
#
# NOT ported (by design — see module brief): framework extensions (ir.*/res.*,
# G-MAIL-6 lives on res.users), wizards, mixins, push devices, RTC/gif/voice/
# previews/translations (link-preview MODELS MAIL-M7/M12 deferred with their
# routes — Increment 3), fetchmail/ir.mail_server (gateway layer, Increment 3),
# mail.template (lives in backbone-notification).
# =============================================================================
