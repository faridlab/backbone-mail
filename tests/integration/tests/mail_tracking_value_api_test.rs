//! MailTrackingValue API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailTrackingValue CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILTRACKINGVALUE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailTrackingValue
pub struct MailTrackingValueTestData;

impl TestDataGenerator for MailTrackingValueTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "mail_message_id": Uuid::new_v4().to_string(),
            "field": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "field_desc": null,
            "field_type": null,
            "old_value_integer": null,
            "old_value_float": null,
            "old_value_text": null,
            "old_value_datetime": null,
            "new_value_integer": null,
            "new_value_float": null,
            "new_value_text": null,
            "new_value_datetime": null,
            "currency_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "mail_message_id": Uuid::new_v4().to_string(),
            "field": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "field_desc": null,
            "field_type": null,
            "old_value_integer": null,
            "old_value_float": null,
            "old_value_text": null,
            "old_value_datetime": null,
            "new_value_integer": null,
            "new_value_float": null,
            "new_value_text": null,
            "new_value_datetime": null,
            "currency_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILTRACKINGVALUE API TEST
// ============================================================================

/// MailTrackingValue API test suite
pub struct MailTrackingValueApiTest {
    inner: GenericCrudTest<MailTrackingValueTestData>,
}

impl MailTrackingValueApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_tracking_values", "MailTrackingValue");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailTrackingValueTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailTrackingValueApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_tracking_value_crud() {
        let mut test = MailTrackingValueApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailTrackingValue specific tests here
// <<< CUSTOM TESTS END >>>
