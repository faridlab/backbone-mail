//! MailMessage API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailMessage CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILMESSAGE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailMessage
pub struct MailMessageTestData;

impl TestDataGenerator for MailMessageTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "subject": null,
            "date": now,
            "body": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "message_type": "email",
            "subtype_id": null,
            "is_internal": false,
            "author_id": null,
            "author_guest_id": null,
            "email_from": null,
            "message_id": null,
            "reply_to": null,
            "model": null,
            "res_id": null,
            "record_name": null,
            "moderation_status": null,
            "needaction": false,
            "has_error": false,
            "failure_reason": null,
            "rating_value": null,
            "pinned_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "subject": null,
            "date": now,
            "body": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "message_type": "email",
            "subtype_id": null,
            "is_internal": false,
            "author_id": null,
            "author_guest_id": null,
            "email_from": null,
            "message_id": null,
            "reply_to": null,
            "model": null,
            "res_id": null,
            "record_name": null,
            "moderation_status": null,
            "needaction": false,
            "has_error": false,
            "failure_reason": null,
            "rating_value": null,
            "pinned_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILMESSAGE API TEST
// ============================================================================

/// MailMessage API test suite
pub struct MailMessageApiTest {
    inner: GenericCrudTest<MailMessageTestData>,
}

impl MailMessageApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_messages", "MailMessage");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailMessageTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailMessageApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_message_crud() {
        let mut test = MailMessageApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailMessage specific tests here
// <<< CUSTOM TESTS END >>>
