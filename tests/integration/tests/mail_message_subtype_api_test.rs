//! MailMessageSubtype API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailMessageSubtype CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILMESSAGESUBTYPE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailMessageSubtype
pub struct MailMessageSubtypeTestData;

impl TestDataGenerator for MailMessageSubtypeTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "description": null,
            "internal": false,
            "parent_id": null,
            "relation_field": null,
            "res_model": null,
            "default": false,
            "hidden": false,
            "tracked": false,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "description": null,
            "internal": false,
            "parent_id": null,
            "relation_field": null,
            "res_model": null,
            "default": false,
            "hidden": false,
            "tracked": false,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILMESSAGESUBTYPE API TEST
// ============================================================================

/// MailMessageSubtype API test suite
pub struct MailMessageSubtypeApiTest {
    inner: GenericCrudTest<MailMessageSubtypeTestData>,
}

impl MailMessageSubtypeApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_message_subtypes", "MailMessageSubtype");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailMessageSubtypeTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailMessageSubtypeApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_message_subtype_crud() {
        let mut test = MailMessageSubtypeApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailMessageSubtype specific tests here
// <<< CUSTOM TESTS END >>>
