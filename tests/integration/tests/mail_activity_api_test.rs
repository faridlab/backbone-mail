//! MailActivity API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailActivity CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILACTIVITY TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailActivity
pub struct MailActivityTestData;

impl TestDataGenerator for MailActivityTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "res_model": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "res_id": Uuid::new_v4().to_string(),
            "res_model_id": null,
            "activity_type_id": null,
            "summary": null,
            "note": null,
            "date_deadline": Utc::now().format("%Y-%m-%d").to_string(),
            "user_id": Uuid::new_v4().to_string(),
            "requested_user_id": null,
            "state": "overdue",
            "active": false,
            "has_recommended_activities": false,
            "chained_next_activity": null,
            "calendar_event_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "res_model": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "res_id": Uuid::new_v4().to_string(),
            "res_model_id": null,
            "activity_type_id": null,
            "summary": null,
            "note": null,
            "date_deadline": Utc::now().format("%Y-%m-%d").to_string(),
            "user_id": Uuid::new_v4().to_string(),
            "requested_user_id": null,
            "state": "overdue",
            "active": false,
            "has_recommended_activities": false,
            "chained_next_activity": null,
            "calendar_event_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILACTIVITY API TEST
// ============================================================================

/// MailActivity API test suite
pub struct MailActivityApiTest {
    inner: GenericCrudTest<MailActivityTestData>,
}

impl MailActivityApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_activities", "MailActivity");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailActivityTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailActivityApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_activity_crud() {
        let mut test = MailActivityApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailActivity specific tests here
// <<< CUSTOM TESTS END >>>
