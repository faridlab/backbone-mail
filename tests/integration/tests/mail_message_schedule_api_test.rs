//! MailMessageSchedule API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailMessageSchedule CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILMESSAGESCHEDULE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailMessageSchedule
pub struct MailMessageScheduleTestData;

impl TestDataGenerator for MailMessageScheduleTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "mail_message_id": Uuid::new_v4().to_string(),
            "notification_parameters": null,
            "scheduled_datetime": now,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "mail_message_id": Uuid::new_v4().to_string(),
            "notification_parameters": null,
            "scheduled_datetime": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILMESSAGESCHEDULE API TEST
// ============================================================================

/// MailMessageSchedule API test suite
pub struct MailMessageScheduleApiTest {
    inner: GenericCrudTest<MailMessageScheduleTestData>,
}

impl MailMessageScheduleApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_message_schedules", "MailMessageSchedule");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailMessageScheduleTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailMessageScheduleApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_message_schedule_crud() {
        let mut test = MailMessageScheduleApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailMessageSchedule specific tests here
// <<< CUSTOM TESTS END >>>
