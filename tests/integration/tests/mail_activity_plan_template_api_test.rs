//! MailActivityPlanTemplate API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailActivityPlanTemplate CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILACTIVITYPLANTEMPLATE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailActivityPlanTemplate
pub struct MailActivityPlanTemplateTestData;

impl TestDataGenerator for MailActivityPlanTemplateTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "plan_id": Uuid::new_v4().to_string(),
            "activity_type_id": Uuid::new_v4().to_string(),
            "summary": null,
            "note": null,
            "delay_count": null,
            "delay_unit": null,
            "user_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "plan_id": Uuid::new_v4().to_string(),
            "activity_type_id": Uuid::new_v4().to_string(),
            "summary": null,
            "note": null,
            "delay_count": null,
            "delay_unit": null,
            "user_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILACTIVITYPLANTEMPLATE API TEST
// ============================================================================

/// MailActivityPlanTemplate API test suite
pub struct MailActivityPlanTemplateApiTest {
    inner: GenericCrudTest<MailActivityPlanTemplateTestData>,
}

impl MailActivityPlanTemplateApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_activity_plan_templates", "MailActivityPlanTemplate");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailActivityPlanTemplateTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailActivityPlanTemplateApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_activity_plan_template_crud() {
        let mut test = MailActivityPlanTemplateApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailActivityPlanTemplate specific tests here
// <<< CUSTOM TESTS END >>>
