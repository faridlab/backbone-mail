//! MailGatewayAllowed API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailGatewayAllowed CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILGATEWAYALLOWED TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailGatewayAllowed
pub struct MailGatewayAllowedTestData;

impl TestDataGenerator for MailGatewayAllowedTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "fetchmail_server_id": null,
            "pattern": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "active": false,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "fetchmail_server_id": null,
            "pattern": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "active": false,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILGATEWAYALLOWED API TEST
// ============================================================================

/// MailGatewayAllowed API test suite
pub struct MailGatewayAllowedApiTest {
    inner: GenericCrudTest<MailGatewayAllowedTestData>,
}

impl MailGatewayAllowedApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_gateway_allowed", "MailGatewayAllowed");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailGatewayAllowedTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailGatewayAllowedApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_gateway_allowed_crud() {
        let mut test = MailGatewayAllowedApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailGatewayAllowed specific tests here
// <<< CUSTOM TESTS END >>>
