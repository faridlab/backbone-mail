//! MailActivityType API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailActivityType CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILACTIVITYTYPE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailActivityType
pub struct MailActivityTypeTestData;

impl TestDataGenerator for MailActivityTypeTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "summary": null,
            "note": null,
            "category": null,
            "chaining_type": "suggest",
            "delay_count": 1,
            "delay_unit": "days",
            "delay_from": "plan_date",
            "default_user_id": null,
            "default_note": null,
            "icon": null,
            "res_model": null,
            "res_model_id": null,
            "active": false,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "summary": null,
            "note": null,
            "category": null,
            "chaining_type": "suggest",
            "delay_count": 1,
            "delay_unit": "days",
            "delay_from": "plan_date",
            "default_user_id": null,
            "default_note": null,
            "icon": null,
            "res_model": null,
            "res_model_id": null,
            "active": false,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILACTIVITYTYPE API TEST
// ============================================================================

/// MailActivityType API test suite
pub struct MailActivityTypeApiTest {
    inner: GenericCrudTest<MailActivityTypeTestData>,
}

impl MailActivityTypeApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_activity_types", "MailActivityType");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailActivityTypeTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailActivityTypeApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_activity_type_crud() {
        let mut test = MailActivityTypeApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailActivityType specific tests here
// <<< CUSTOM TESTS END >>>
