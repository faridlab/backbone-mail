//! MailAliasDomain API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailAliasDomain CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILALIASDOMAIN TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailAliasDomain
pub struct MailAliasDomainTestData;

impl TestDataGenerator for MailAliasDomainTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "catchall_alias": null,
            "bounce_alias": null,
            "bounce_alias_email": null,
            "catchall_alias_email": null,
            "use_mx": false,
            "mail_server_id": null,
            "from_filter": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "catchall_alias": null,
            "bounce_alias": null,
            "bounce_alias_email": null,
            "catchall_alias_email": null,
            "use_mx": false,
            "mail_server_id": null,
            "from_filter": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILALIASDOMAIN API TEST
// ============================================================================

/// MailAliasDomain API test suite
pub struct MailAliasDomainApiTest {
    inner: GenericCrudTest<MailAliasDomainTestData>,
}

impl MailAliasDomainApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_alias_domains", "MailAliasDomain");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailAliasDomainTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailAliasDomainApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_alias_domain_crud() {
        let mut test = MailAliasDomainApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailAliasDomain specific tests here
// <<< CUSTOM TESTS END >>>
