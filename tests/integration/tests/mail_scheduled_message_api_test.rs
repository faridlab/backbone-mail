//! MailScheduledMessage API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailScheduledMessage CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILSCHEDULEDMESSAGE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailScheduledMessage
pub struct MailScheduledMessageTestData;

impl TestDataGenerator for MailScheduledMessageTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "subject": null,
            "body": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "scheduled_date": now,
            "composition_comment_option": null,
            "model": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "res_id": Uuid::new_v4().to_string(),
            "author_party_id": Uuid::new_v4().to_string(),
            "recipient_party_ids": null,
            "is_note": false,
            "notification_parameters": null,
            "send_context": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "subject": null,
            "body": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "scheduled_date": now,
            "composition_comment_option": null,
            "model": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "res_id": Uuid::new_v4().to_string(),
            "author_party_id": Uuid::new_v4().to_string(),
            "recipient_party_ids": null,
            "is_note": false,
            "notification_parameters": null,
            "send_context": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILSCHEDULEDMESSAGE API TEST
// ============================================================================

/// MailScheduledMessage API test suite
pub struct MailScheduledMessageApiTest {
    inner: GenericCrudTest<MailScheduledMessageTestData>,
}

impl MailScheduledMessageApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_scheduled_messages", "MailScheduledMessage");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailScheduledMessageTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailScheduledMessageApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_scheduled_message_crud() {
        let mut test = MailScheduledMessageApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailScheduledMessage specific tests here
// <<< CUSTOM TESTS END >>>
