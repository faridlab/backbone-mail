//! MailMessageReaction API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailMessageReaction CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILMESSAGEREACTION TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailMessageReaction
pub struct MailMessageReactionTestData;

impl TestDataGenerator for MailMessageReactionTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "message_id": Uuid::new_v4().to_string(),
            "content": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "partner_id": null,
            "guest_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "message_id": Uuid::new_v4().to_string(),
            "content": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "partner_id": null,
            "guest_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILMESSAGEREACTION API TEST
// ============================================================================

/// MailMessageReaction API test suite
pub struct MailMessageReactionApiTest {
    inner: GenericCrudTest<MailMessageReactionTestData>,
}

impl MailMessageReactionApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_message_reactions", "MailMessageReaction");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailMessageReactionTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailMessageReactionApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_message_reaction_crud() {
        let mut test = MailMessageReactionApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailMessageReaction specific tests here
// <<< CUSTOM TESTS END >>>
