//! MailFollowers API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the MailFollowers CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// MAILFOLLOWERS TEST DATA GENERATOR
// ============================================================================

/// Test data generator for MailFollowers
pub struct MailFollowersTestData;

impl TestDataGenerator for MailFollowersTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "res_model": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "res_id": Uuid::new_v4().to_string(),
            "partner_id": null,
            "channel_id": null,
            "subtype_ids": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "res_model": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "res_id": Uuid::new_v4().to_string(),
            "partner_id": null,
            "channel_id": null,
            "subtype_ids": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// MAILFOLLOWERS API TEST
// ============================================================================

/// MailFollowers API test suite
pub struct MailFollowersApiTest {
    inner: GenericCrudTest<MailFollowersTestData>,
}

impl MailFollowersApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/mail_followers", "MailFollowers");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, MailFollowersTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for MailFollowersApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_mail_followers_crud() {
        let mut test = MailFollowersApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add MailFollowers specific tests here
// <<< CUSTOM TESTS END >>>
